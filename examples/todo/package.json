{
  "name": "todo",
  "version": "1.0.0",
  "description": "Example todo app for Credit framework",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development node src/index.html.js >src/index.html && sirv src/",
    "build": "cp -r src dist && node src/index.html.js >dist/index.html"
  },
  "dependencies": {
    "credit": "workspace:*"
  },
  "devDependencies": {
    "sirv-cli": "^2.0.2",
    "tsconfig": "workspace:*"
  }
}
